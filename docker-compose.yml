version: '3.8'

services:
  plex-cleanup:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plex-cleanup
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
